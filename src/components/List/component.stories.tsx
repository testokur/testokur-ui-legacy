import * as React from 'react';
import { List, ListItem } from '.';

export default {
  title: 'List',
};

export const Default = (): JSX.Element => {
  return (
    <List>
      <ListItem />
    </List>
  );
};
