export type Opacity = {
  overlay: number;
  buttonDisabled: number;
  radioButtonDisabled: number;
  checkboxDisabled: number;
};

const opacity = {
  overlay: 0.8,
  buttonDisabled: 0.3,
  radioButtonDisabled: 0.5,
  checkboxDisabled: 0.5,
};

export default opacity;
