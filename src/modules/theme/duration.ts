export type Duration = {
  durationFast: string;
  durationNormal: string;
  durationSlow: string;
};

const duration = {
  durationFast: '0.15s',
  durationNormal: '0.3s',
  durationSlow: '0.4s',
};

export default duration;
